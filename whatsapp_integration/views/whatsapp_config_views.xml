<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- WhatsApp Configuration Form View -->
    <record id="view_whatsapp_config_form" model="ir.ui.view">
        <field name="name">whatsapp.config.form</field>
        <field name="model">whatsapp.config</field>
        <field name="arch" type="xml">
            <form string="WhatsApp Configuration">
                <header>
                    <button name="test_connection" string="Test Connection" type="object" class="oe_highlight"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only"/>
                        <h1><field name="name" placeholder="e.g., Production WhatsApp API"/></h1>
                    </div>
                    <group>
                        <group string="Meta WhatsApp API Credentials">
                            <field name="is_active"/>
                            <field name="whatsapp_phone_number_id"/>
                            <field name="whatsapp_business_account_id"/>
                            <field name="access_token" password="1"/>
                            <field name="api_version"/>
                        </group>
                        <group string="Webhook Configuration">
                            <field name="webhook_verify_token" password="1"/>
                            <div class="text-muted">
                                <p>Set up a webhook at Meta Developer dashboard with this URL:</p>
                                <p><strong>Callback URL:</strong> [Your Odoo URL]/whatsapp/webhook</p>
                                <p><strong>Verify Token:</strong> Use the token defined above</p>
                                <p><strong>Required fields:</strong> messages, message_deliveries, message_reads</p>
                            </div>
                        </group>
                    </group>
                    <group string="Default Settings">
                        <field name="default_message_template_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- WhatsApp Configuration Tree View -->
    <record id="view_whatsapp_config_tree" model="ir.ui.view">
        <field name="name">whatsapp.config.tree</field>
        <field name="model">whatsapp.config</field>
        <field name="arch" type="xml">
            <tree string="WhatsApp Configurations">
                <field name="name"/>
                <field name="whatsapp_phone_number_id"/>
                <field name="is_active"/>
            </tree>
        </field>
    </record>

    <!-- WhatsApp Configuration Action -->
    <record id="action_whatsapp_config" model="ir.actions.act_window">
        <field name="name">WhatsApp Configurations</field>
        <field name="res_model">whatsapp.config</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first WhatsApp configuration
            </p>
            <p>
                Configure your WhatsApp Business API credentials to enable sending and receiving messages.
            </p>
        </field>
    </record>
</odoo>
